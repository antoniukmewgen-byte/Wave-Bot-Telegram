python-telegram-bot==21.5
fastapi==0.110.0
uvicorn==0.29.0
# gspread закріплено на 6.0.x навмисно — gspread-asyncio==2.0.0 жорстко
# вимагає gspread==6.0.* (див. його метадані залежностей), новіший gspread зламає імпорт.
gspread==6.0.2
gspread-asyncio==2.0.0
google-auth==2.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
aiohttp==3.9.5
# Прод-сервер (EC2, Amazon Linux) досі на Python 3.9. tenacity>=9.1.3 та
# pydantic-settings>=2.12.0 підняли мінімальну версію до Python >=3.10 —
# на 3.9 pip не знаходить під них жодного дистрибутиву (перевірено 2026-08-01).
# Тому нижче навмисно закріплені останні версії, що ще підтримують 3.9.
# API, яке ми використовуємо (retry/stop/wait/before_sleep у kommo.py;
# BaseSettings/SettingsConfigDict/Field у config.py), в новіших релізах не мінялось.
tenacity==9.1.2
apscheduler==3.11.3
pydantic-settings==2.11.0
aiosqlite==0.22.1
# phonenumbers/tzdata — визначення часового поясу клієнта по номеру телефону
# (фіча "ранкові ліди": не видаємо заявку менеджеру до 9:00 за місцевим часом клієнта).
# Обидві — чисто Python (без C-розширень), тому підтримка Python 3.9 не проблема.
phonenumbers==8.13.55
tzdata==2025.2
